project.name = cashwarden
project.default_charset = utf-8
server.index = 8383
server.search = 8384

[id]
type = id

[content]
type = body

[tags]
type = string

[date]
type = numeric

[user_id]
index = self
tokenizer = full

[ledger_id]
index = self
tokenizer = full

[category_id]
index = self
tokenizer = full
